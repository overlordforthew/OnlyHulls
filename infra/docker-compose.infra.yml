version: "3.8"

services:
  datemyboat-db:
    image: pgvector/pgvector:pg17
    container_name: datemyboat-db
    restart: unless-stopped
    environment:
      POSTGRES_DB: datemyboat
      POSTGRES_USER: datemyboat
      POSTGRES_PASSWORD: ${DATEMYBOAT_DB_PASSWORD:-changeme}
    volumes:
      - datemyboat-db-data:/var/lib/postgresql/data
    ports:
      - "127.0.0.1:5433:5432"
    networks:
      - coolify
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U datemyboat"]
      interval: 10s
      timeout: 5s
      retries: 5

  datemyboat-meilisearch:
    image: getmeili/meilisearch:v1.12
    container_name: datemyboat-meilisearch
    restart: unless-stopped
    environment:
      MEILI_MASTER_KEY: ${MEILISEARCH_API_KEY:-changeme_meili_master_key}
      MEILI_ENV: production
    volumes:
      - datemyboat-meili-data:/meili_data
    ports:
      - "127.0.0.1:7701:7700"
    networks:
      - coolify
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--spider", "http://localhost:7700/health"]
      interval: 10s
      timeout: 5s
      retries: 5

  datemyboat-redis:
    image: redis:7-alpine
    container_name: datemyboat-redis
    restart: unless-stopped
    command: redis-server --maxmemory 64mb --maxmemory-policy allkeys-lru
    volumes:
      - datemyboat-redis-data:/data
    ports:
      - "127.0.0.1:6380:6379"
    networks:
      - coolify
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  datemyboat-db-data:
  datemyboat-meili-data:
  datemyboat-redis-data:

networks:
  coolify:
    external: true
